# 1. import pandas, joblib, sklearn pipeline, models, preprocessing
# 2. load X_train, y_train
# 3. build pipeline:
#      - SimpleImputer (median)
#      - StandardScaler
#      - RandomForestClassifier
# 4. fit pipeline on train data
# 5. save trained model to outputs/models/best_model.pkl
