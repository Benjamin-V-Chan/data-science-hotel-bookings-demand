# Load cleaned dataset
# Select relevant features (numerical and encoded categorical)
# Train/test split
# Train a RandomForestClassifier to predict is_canceled
# Evaluate accuracy and feature importance
# Save results and model to outputs/model_results/
